
public class Arvore {
	private No Raiz;
	
	public void Insira(int valor) {
		Raiz = InserirNo(Raiz, valor);
	}
	
	private No InserirNo(No noAtual, int valor) {
		if (noAtual == null) {
			return new No(valor);
		}
		return new No(valor);
	}
}
